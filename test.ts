// Erro: Nome da variável deve ser lowerCamelCase
const minhaVariavel = 10;

// Erro: Falta tipo de retorno explícito na função (corrigido)
function soma(a: number, b: number): number {
    return a + b;
}

// Erro: Uso de "$" não permitido
const valorInvalido = 100;

// Erro: Enum em PascalCase em vez de UPPER_CASE (corrigido)
enum Status {
    ATIVO = "ATIVO",
    INATIVO = "INATIVO"
}
